slug: rehold
name: Rehold
url: https://rehold.com
category: public_records
priority: medium
data_types:
  - name
  - address
  - property_records
opt_out:
  methods:
    - type: form
      url: https://rehold.com/opt-out
      steps:
        - action: goto
          url: https://rehold.com/opt-out
        - action: fill
          selector: "input[name=first_name]"
          field: first_name
        - action: fill
          selector: "input[name=last_name]"
          field: last_name
        - action: fill
          selector: "input[name=email]"
          field: email
        - action: click
          selector: "button[type=submit]"
        - action: screenshot
          name: rehold_submission
  verification:
    type: check_listing
    check_url: https://rehold.com
    expected_days: 14
  reappearance:
    frequency_days: 90
notes: "Property records broker. Opt-out form requires CAPTCHA completion."
